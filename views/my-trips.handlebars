<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Trips</title>
</head>
<body>
    {{!-- We could possibly make this look better by just showing the user a limited amount of information, because as it is right now, it shows the same information as when they select their trip --}}
    <h1>Welcome, {{user.username}}!</h1>
    <h2>Your Trips:</h2>
        {{#each user.trips}}
        <section class="blog-container">
        <a href="api/trips/{{this.id}}">
            <ul>
                    <li>
                        <strong><h3>Trip To {{this.city}}, {{this.state}}</h3></strong><br>
                        <strong>Activities:</strong>
                        <ul>
                            {{#each this.activities}}
                                <strong><li>{{this.activity}}</li></strong>
                                <li>{{this.description}}</li>
                            {{/each}}
                        </ul>
                        <strong>Accessories:</strong> 
                        <ul>
                            {{#each this.accessories.items}}
                                <li>{{this}}</li>
                            {{/each}}
                        </ul>
                        <strong>Fun Fact:</strong> {{this.funFact}}<br>
                        <strong>City:</strong> {{this.city}}<br>
                        <strong>State:</strong> {{this.state}}<br>
                        <strong>Month:</strong> {{this.month}}<br>
                    </li>
            </ul>
        </a>   
        <button id="click" class="btn-delete text-2xl text-black text-shadow" data-id="{{this.id}}" style="background-color:#99F2CD">Delete </button>
    </section>
        {{/each}}
</body>
</html>